extends layout

block head
  title Trop d'envoi de mails

block content
  h1 Trop d'envoi de mails
  div
    | Plusieurs mails vous ont déjà été envoyés. Merci de patienter pour leur bonne réception, et de vérifier votre
    | boîte de spam. Vous pourrez alors saisir votre code ci-dessous.
  form(method='POST' action='connexion')
    input(type="hidden" name="username" value=userId)
    input(type="hidden" name="csrf" value=csrf)
    div(class=error ? "form-group has-error" : "form-group")
      label(for="password") Code de connexion&nbsp;:
      .input-group
        input(type="text" class="form-control input-lg text-center" name="password")
        .input-group-btn
          button(type="submit" class="btn btn-lg btn-primary") Se connecter
      if error
        span(class="help-block")= error
